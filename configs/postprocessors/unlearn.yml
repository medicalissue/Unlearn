postprocessor:
  name: unlearn
  postprocessor_args:
    unlearn_mode: "ascent"
    eta: 1e-2
    num_steps: 100
    temp: 1.0

    score_type: "prototype_coupling"

    prototype_coupling_config:
      use_all_prototypes: false
      top_k: 5
      eigenvalue_mode: "participation_ratio"